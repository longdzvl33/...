local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")

local Player = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local Active = false
local Speed = 1
local FOV = 70

local SavedType
local SavedSubject
local CamPosition = Camera.CFrame.Position
local RotationX = 0
local RotationY = 0

local Speeds = {1, 2, 5, 10, 20, 0.5}
local SpeedIndex = 1

local Up = false
local Down = false
local Hidden = false

local Gui = Instance.new("ScreenGui")
Gui.Name = "FreecamGui"
Gui.ResetOnSpawn = false
Gui.IgnoreGuiInset = true
Gui.Parent = game:GetService("CoreGui")

local Main = Instance.new("Frame")
Main.Size = UDim2.fromOffset(180, 235)
Main.Position = UDim2.new(0.05, 0, 0.3, 0)
Main.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true
Main.Parent = Gui

Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 12)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.fromOffset(80, 30)
Title.Position = UDim2.fromOffset(10, 5)
Title.BackgroundTransparency = 1
Title.Text = "FREECAM"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 16
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Main

local Hide = Instance.new("TextButton")
Hide.Size = UDim2.fromOffset(40, 30)
Hide.Position = UDim2.new(1, -75, 0, 5)
Hide.BackgroundTransparency = 1
Hide.Text = "ẨN"
Hide.TextColor3 = Color3.fromRGB(120, 120, 120)
Hide.Font = Enum.Font.SourceSansBold
Hide.TextSize = 13
Hide.Parent = Main

local Dot = Instance.new("TextButton")
Dot.Size = UDim2.fromOffset(16, 16)
Dot.Position = UDim2.new(1, -26, 0, 12)
Dot.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
Dot.BorderSizePixel = 0
Dot.Text = ""
Dot.Parent = Main

Instance.new("UICorner", Dot).CornerRadius = UDim.new(1, 0)

local FOVBox = Instance.new("TextBox")
FOVBox.Size = UDim2.new(1, 0, 0, 20)
FOVBox.Position = UDim2.fromOffset(0, 40)
FOVBox.BackgroundTransparency = 1
FOVBox.Text = "FOV: 70"
FOVBox.TextColor3 = Color3.fromRGB(0, 120, 255)
FOVBox.Font = Enum.Font.SourceSansBold
FOVBox.TextSize = 14
FOVBox.ClearTextOnFocus = false
FOVBox.TextXAlignment = Enum.TextXAlignment.Center
FOVBox.Parent = Main

local FOVSlider = Instance.new("TextButton")
FOVSlider.Size = UDim2.fromOffset(140, 6)
FOVSlider.Position = UDim2.fromOffset(20, 65)
FOVSlider.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
FOVSlider.BorderSizePixel = 0
FOVSlider.Text = ""
FOVSlider.AutoButtonColor = false
FOVSlider.Parent = Main

local FOVProgress = Instance.new("Frame")
FOVProgress.Size = UDim2.new((FOV - 30) / 90, 0, 1, 0)
FOVProgress.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
FOVProgress.BorderSizePixel = 0
FOVProgress.Parent = FOVSlider

local Status = Instance.new("TextButton")
Status.Size = UDim2.fromOffset(150, 38)
Status.Position = UDim2.fromOffset(15, 85)
Status.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Status.Text = "TRẠNG THÁI: TẮT"
Status.TextColor3 = Color3.new(1, 1, 1)
Status.Font = Enum.Font.SourceSansBold
Status.TextSize = 15
Status.Parent = Main

Instance.new("UICorner", Status).CornerRadius = UDim.new(0, 6)

local SpeedButton = Instance.new("TextButton")
SpeedButton.Size = UDim2.fromOffset(150, 38)
SpeedButton.Position = UDim2.fromOffset(15, 130)
SpeedButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
SpeedButton.Text = "TỐC ĐỘ: 1x"
SpeedButton.TextColor3 = Color3.new(1, 1, 1)
SpeedButton.Font = Enum.Font.SourceSansBold
SpeedButton.TextSize = 15
SpeedButton.Parent = Main

Instance.new("UICorner", SpeedButton).CornerRadius = UDim.new(0, 6)

local Teleport = Instance.new("TextButton")
Teleport.Size = UDim2.fromOffset(150, 38)
Teleport.Position = UDim2.fromOffset(15, 175)
Teleport.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Teleport.Text = "DỊCH CHUYỂN ĐẾN ĐÂY"
Teleport.TextColor3 = Color3.new(1, 1, 1)
Teleport.Font = Enum.Font.SourceSansBold
Teleport.TextSize = 13
Teleport.Parent = Main

Instance.new("UICorner", Teleport).CornerRadius = UDim.new(0, 6)

local UpDown = Instance.new("Frame")
UpDown.Size = UDim2.fromOffset(100, 120)
UpDown.Position = UDim2.new(1, -115, 1, -150)
UpDown.BackgroundTransparency = 1
UpDown.Visible = false
UpDown.Parent = Gui

local UpButton = Instance.new("TextButton")
UpButton.Size = UDim2.fromOffset(90, 52)
UpButton.Position = UDim2.fromOffset(5, 0)
UpButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
UpButton.Text = "LÊN"
UpButton.TextColor3 = Color3.new(1, 1, 1)
UpButton.Font = Enum.Font.SourceSansBold
UpButton.TextSize = 18
UpButton.Parent = UpDown

Instance.new("UICorner", UpButton).CornerRadius = UDim.new(0, 8)

local DownButton = Instance.new("TextButton")
DownButton.Size = UDim2.fromOffset(90, 52)
DownButton.Position = UDim2.fromOffset(5, 62)
DownButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
DownButton.Text = "XUỐNG"
DownButton.TextColor3 = Color3.new(1, 1, 1)
DownButton.Font = Enum.Font.SourceSansBold
DownButton.TextSize = 18
DownButton.Parent = UpDown

Instance.new("UICorner", DownButton).CornerRadius = UDim.new(0, 8)

local ShowCircle = Instance.new("TextButton")
ShowCircle.Size = UDim2.fromOffset(50, 50)
ShowCircle.Position = UDim2.new(0.05, 0, 0.3, 0)
ShowCircle.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
ShowCircle.BorderSizePixel = 0
ShowCircle.Text = "CAM"
ShowCircle.TextColor3 = Color3.new(1, 1, 1)
ShowCircle.Font = Enum.Font.SourceSansBold
ShowCircle.TextSize = 14
ShowCircle.Visible = false
ShowCircle.Active = true
ShowCircle.Draggable = true
ShowCircle.Parent = Gui

Instance.new("UICorner", ShowCircle).CornerRadius = UDim.new(1, 0)

local function GetRoot()
    local Character = Player.Character
    if not Character then return nil end
    return Character:FindFirstChild("HumanoidRootPart")
end

local function SetAnchor(Value)
    local Root = GetRoot()
    if Root then
        Root.Anchored = Value
    end
end

local function SetJumpVisible(Value)
    local PlayerGui = Player:FindFirstChildOfClass("PlayerGui")
    if not PlayerGui then return end

    local TouchGui = PlayerGui:FindFirstChild("TouchGui")
    if not TouchGui then return end

    local TouchControlFrame = TouchGui:FindFirstChild("TouchControlFrame")
    if not TouchControlFrame then return end

    local JumpButton = TouchControlFrame:FindFirstChild("JumpButton")
    if JumpButton then
        JumpButton.Visible = Value
    end
end

local function UpdateFOV(Value)
    Value = math.clamp(math.floor(Value), 1, 120)

    FOV = Value
    Camera.FieldOfView = FOV

    FOVBox.Text = "FOV: " .. FOV

    local Percent = math.clamp((FOV - 30) / 90, 0, 1)
    FOVProgress.Size = UDim2.new(Percent, 0, 1, 0)
end

FOVBox.Focused:Connect(function()
    FOVBox.BackgroundTransparency = 0
    FOVBox.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    FOVBox.Text = tostring(FOV)
end)

FOVBox.FocusLost:Connect(function()
    local Value = tonumber(FOVBox.Text)

    if Value then
        UpdateFOV(Value)
    else
        FOVBox.Text = "FOV: " .. FOV
    end

    FOVBox.BackgroundTransparency = 1
end)

local function ToggleFreecam()
    Active = not Active

    if Active then
        Status.Text = "TRẠNG THÁI: BẬT"
        Status.BackgroundColor3 = Color3.fromRGB(16, 128, 55)

        SavedType = Camera.CameraType
        SavedSubject = Camera.CameraSubject
        CamPosition = Camera.CFrame.Position

        local X, Y, Z = Camera.CFrame:ToOrientation()
        RotationX = Y
        RotationY = X

        Camera.CameraType = Enum.CameraType.Scriptable

        SetAnchor(true)
        SetJumpVisible(false)
        UpDown.Visible = true
    else
        Status.Text = "TRẠNG THÁI: TẮT"
        Status.BackgroundColor3 = Color3.fromRGB(30, 30, 30)

        Camera.CameraType = SavedType or Enum.CameraType.Custom
        Camera.CameraSubject = SavedSubject

        SetAnchor(false)

        Up = false
        Down = false
        UpDown.Visible = false

        SetJumpVisible(true)
    end
end

Status.MouseButton1Click:Connect(ToggleFreecam)

SpeedButton.MouseButton1Click:Connect(function()
    SpeedIndex += 1

    if SpeedIndex > #Speeds then
        SpeedIndex = 1
    end

    Speed = Speeds[SpeedIndex]
    SpeedButton.Text = "TỐC ĐỘ: " .. Speed .. "x"
end)

FOVSlider.MouseButton1Click:Connect(function()
    local MousePosition = UIS:GetMouseLocation()

    local Percent = math.clamp(
        (MousePosition.X - FOVSlider.AbsolutePosition.X)
        / FOVSlider.AbsoluteSize.X,
        0,
        1
    )

    UpdateFOV(math.floor(30 + Percent * 90))
end)

local function SetUp(Value)
    Up = Value
end

local function SetDown(Value)
    Down = Value
end

UpButton.MouseButton1Down:Connect(function()
    SetUp(true)
end)

UpButton.MouseButton1Up:Connect(function()
    SetUp(false)
end)

DownButton.MouseButton1Down:Connect(function()
    SetDown(true)
end)

DownButton.MouseButton1Up:Connect(function()
    SetDown(false)
end)

UpButton.MouseLeave:Connect(function()
    SetUp(false)
end)

DownButton.MouseLeave:Connect(function()
    SetDown(false)
end)

Teleport.MouseButton1Click:Connect(function()
    local Root = GetRoot()
    if not Root then return end

    local WasAnchored = Root.Anchored

    Root.Anchored = false
    Root.CFrame = CFrame.new(Camera.CFrame.Position)

    if Active or WasAnchored then
        Root.Anchored = true
    end
end)

local function UpdateHiddenState()
    Main.Visible = not Hidden
    ShowCircle.Visible = Hidden

    if Active then
        UpDown.Visible = true
        SetJumpVisible(false)
    else
        UpDown.Visible = false
        SetJumpVisible(true)
    end
end

local function ToggleMenu()
    Hidden = not Hidden
    UpdateHiddenState()
end

Hide.MouseButton1Click:Connect(ToggleMenu)
Dot.MouseButton1Click:Connect(ToggleMenu)
ShowCircle.MouseButton1Click:Connect(ToggleMenu)

UIS.InputChanged:Connect(function(Input)
    if not Active then return end

    if Input.UserInputType == Enum.UserInputType.Touch
        or Input.UserInputType == Enum.UserInputType.MouseMovement then

        local Sensitivity = 0.005

        RotationX -= Input.Delta.X * Sensitivity
        RotationY -= Input.Delta.Y * Sensitivity

        RotationY = math.clamp(
            RotationY,
            -math.rad(80),
            math.rad(80)
        )
    end
end)

RunService.RenderStepped:Connect(function(DeltaTime)
    if not Active then return end

    Camera.FieldOfView = FOV

    local Rotation =
        CFrame.Angles(0, RotationX, 0) *
        CFrame.Angles(RotationY, 0, 0)

    local Character = Player.Character
    local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")

    if Humanoid then
        local Move = Humanoid.MoveDirection

        if Move.Magnitude > 0 then
            local Forward = Camera.CFrame.LookVector
            local Right = Camera.CFrame.RightVector

            local FlatForward = Vector3.new(
                Forward.X,
                0,
                Forward.Z
            )

            local FlatRight = Vector3.new(
                Right.X,
                0,
                Right.Z
            )

            if FlatForward.Magnitude > 0 then
                FlatForward = FlatForward.Unit
            end

            if FlatRight.Magnitude > 0 then
                FlatRight = FlatRight.Unit
            end

            local ForwardInput = Move:Dot(FlatForward)
            local SideInput = Move:Dot(FlatRight)

            local FlyDirection =
                FlatForward * ForwardInput +
                FlatRight * SideInput

            if FlyDirection.Magnitude > 0 then
                CamPosition +=
                    FlyDirection.Unit *
                    Speed *
                    60 *
                    DeltaTime
            end
        end
    end

    if Up then
        CamPosition += Vector3.new(
            0,
            Speed * 60 * DeltaTime,
            0
        )
    end

    if Down then
        CamPosition -= Vector3.new(
            0,
            Speed * 60 * DeltaTime,
            0
        )
    end

    Camera.CFrame = CFrame.new(CamPosition) * Rotation
end)

Player.CharacterAdded:Connect(function()
    task.wait(1)

    if Active then
        SetAnchor(true)
        SetJumpVisible(false)
    else
        SetJumpVisible(true)
    end
end)
